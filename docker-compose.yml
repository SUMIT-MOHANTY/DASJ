version: '3.8'
services:
  backend:
    build: ./backend
    env_file: ./backend/.env.example
    ports:
      - '4000:4000'
    depends_on: []
  frontend:
    build: ./frontend
    ports:
      - '80:80'
    environment:
      - VITE_API_BASE_URL=http://localhost:4000/api

