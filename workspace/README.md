# Project README (excerpt)

## Swagger UI (`/docs`)
The API documentation is available at `https://<host>/docs`.
It uses a placeholder base URL defined by the environment variable `DOCS_URL` (default `https://placeholder.docs`).
After deployment, update `DOCS_URL` to the real public address and request a security review.
